# ================================================================
# ResidEase — Environment Variables
# ================================================================
# HOW TO USE:
#   1. Rename this file from "env.example" to ".env"
#   2. Fill in your real values below
#   3. NEVER commit .env to Git — it is in .gitignore
# ================================================================

# ── Application ──────────────────────────────────────────────────
APP_NAME=ResidEase
APP_ENV=development
APP_PORT=8080
DEBUG=True

# ── Database ─────────────────────────────────────────────────────
# PostgreSQL (recommended for production)
#change this for the no sql database
DATABASE_URL=postgresql://user:password@localhost:5432/residease_db

# SQLite (quick local dev — no install needed)
# DATABASE_URL=sqlite:///./residease.db

# ── JWT ──────────────────────────────────────────────────────────
# Generate a secure secret key by running this in your terminal:
#   python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
JWT_SECRET=your-base64-encoded-secret-key-here
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=604800000

# ── Email (SMTP) ──────────────────────────────────────────────────
MAIL_USERNAME=your@email.com
MAIL_PASSWORD=your-email-app-password
MAIL_FROM=noreply@residease.com
MAIL_FROM_NAME=ResidEase
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_TLS=True
MAIL_SSL=False

# ── File Storage ──────────────────────────────────────────────────
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=10
ALLOWED_EXTENSIONS=jpg,jpeg,png,pdf,docx

# ── CORS ─────────────────────────────────────────────────────────
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ── Payment Gateway ───────────────────────────────────────────────
PAYMENT_GATEWAY_API_KEY=your-payment-gateway-key
PAYMENT_GATEWAY_SECRET=your-payment-gateway-secret

# ── APScheduler (Cron Jobs) ───────────────────────────────────────
BILLING_CRON=0 0 1 * *
REMINDER_CRON=0 9 * * *
LEASE_CHECK_CRON=0 8 * * *

# ── SMS / Twilio (optional) ───────────────────────────────────────
SMS_ACCOUNT_SID=your-twilio-account-sid
SMS_AUTH_TOKEN=your-twilio-auth-token
SMS_FROM_NUMBER=+1234567890